# Создание файла .env

## Автоматически

### Windows (PowerShell)

```powershell
Copy-Item .env.example .env
notepad .env
```

### Linux/Mac

```bash
cp .env.example .env
nano .env
```

## Вручную

Создайте файл `.env` в корне проекта `ruletka/` со следующим содержимым:

```env
# Backend API URL
VITE_API_URL=http://localhost:8080
```

## Настройка

### Локальная разработка

Используйте значение по умолчанию:

```env
VITE_API_URL=http://localhost:8080
```

### Другой хост/порт

Если бекенд запущен на другом порту:

```env
VITE_API_URL=http://localhost:3000
```

Или на другом хосте:

```env
VITE_API_URL=http://192.168.1.100:8080
```

### Production

Для продакшена укажите реальный URL:

```env
VITE_API_URL=https://api.your-domain.com
```

## Проверка

После создания `.env`:

1. Перезапустите dev сервер
2. Проверьте в DevTools Console:
   ```javascript
   console.log(import.meta.env.VITE_API_URL)
   ```
3. Должен вывести ваш URL

## Важно

- ⚠️ `.env` файл **НЕ** должен коммититься в git
- ✅ `.env.example` можно коммитить (без секретов)
- 🔄 После изменения `.env` нужно перезапустить dev сервер

## Устранение проблем

### "import.meta.env.VITE_API_URL is undefined"

**Причина:** Файл `.env` не создан или неправильное имя переменной

**Решение:**
1. Создайте `.env` файл
2. Убедитесь что переменная начинается с `VITE_`
3. Перезапустите dev сервер

### Изменения не применяются

**Решение:**
1. Остановите dev сервер (Ctrl+C)
2. Запустите снова: `npm run dev`

### .env не читается

**Проверьте:**
1. Файл называется именно `.env` (не `.env.txt`)
2. Файл находится в корне проекта `ruletka/`
3. Dev сервер перезапущен после создания файла

---

**Готово!** Теперь приложение будет использовать ваш бекенд API.

