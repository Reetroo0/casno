# 🚀 Быстрый запуск - Шпаргалка

## 📋 Подготовка (один раз)

```bash
# 1. Установить зависимости
cd ruletka
npm install

# 2. Создать .env
Copy-Item .env.example .env    # Windows PowerShell
# или
cp .env.example .env            # Linux/Mac

# 3. Запустить бекенд
cd ../ruletka-backend
docker-compose up -d            # Запустить PostgreSQL
make run                        # Запустить API
```

## ▶️ Ежедневный запуск

### Вариант 1: Онлайн режим (с бекендом)

```bash
# Терминал 1: Backend
cd ruletka-backend
make run

# Терминал 2: Frontend
cd ruletka
npm run dev
```

### Вариант 2: Оффлайн режим (без бекенда)

```bash
# Только фронтенд
cd ruletka
npm run dev
```

## 🎮 Использование

### Онлайн режим
1. Откройте http://localhost:5173
2. Нажмите "🔐 Войти"
3. Зарегистрируйтесь
4. Пополните баланс: "💰 Пополнить"
5. Играйте!

### Оффлайн режим
1. Откройте http://localhost:5173
2. Просто играйте! 🎰

## 🛠️ Полезные команды

```bash
# Type checking
npm run type-check

# Build для продакшена
npm run build

# Предпросмотр продакшн билда
npm run preview

# Очистить кеш
npm run clean
```

## 🔍 Проверка здоровья

### Проверить бекенд
```bash
curl http://localhost:8080/health
# Ожидается: {"status":"healthy"}
```

### Проверить фронтенд
Откройте: http://localhost:5173

### Проверить PostgreSQL
```bash
docker ps
# Должен быть контейнер с postgres
```

## 🐛 Быстрое решение проблем

### Бекенд не запускается
```bash
# Проверить порты
netstat -ano | findstr :8080    # Windows
lsof -i :8080                   # Linux/Mac

# Перезапустить PostgreSQL
docker-compose down
docker-compose up -d
```

### Фронтенд не запускается
```bash
# Очистить и переустановить
rm -rf node_modules package-lock.json
npm install
```

### CORS ошибки
```bash
# Проверить VITE_API_URL в .env
cat .env

# Перезапустить dev сервер
# Ctrl+C, затем npm run dev
```

## 📚 Документация

| Что нужно | Файл |
|-----------|------|
| Настройка API | [SETUP_API.md](SETUP_API.md) |
| Создание .env | [CREATE_ENV.md](CREATE_ENV.md) |
| API документация | [API_INTEGRATION.md](API_INTEGRATION.md) |
| Тестирование | [TESTING_GUIDE.md](TESTING_GUIDE.md) |
| Полное резюме | [INTEGRATION_SUMMARY.md](INTEGRATION_SUMMARY.md) |

## 🎯 Первый раз?

Начните с [SETUP_API.md](SETUP_API.md) для подробных инструкций.

## ⚡ Самые частые команды

```bash
# Запустить все (в двух терминалах)
cd ruletka-backend && make run    # Terminal 1
cd ruletka && npm run dev          # Terminal 2

# Остановить все
# Ctrl+C в обоих терминалах
```

---

**Готово! Приятной игры! 🎰✨**

