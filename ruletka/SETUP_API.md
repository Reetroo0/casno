# Быстрая настройка API интеграции

## 📦 Установка

Все зависимости уже установлены, включая `axios`.

## ⚙️ Настройка

### Шаг 1: Создайте файл `.env`

В корне проекта `ruletka/` создайте файл `.env`:

**Windows (PowerShell):**
```powershell
Copy-Item .env.example .env
```

**Linux/Mac:**
```bash
cp .env.example .env
```

### Шаг 2: Настройте URL бекенда

Откройте `.env` и при необходимости измените URL:

```env
VITE_API_URL=http://localhost:8080
```

Если бекенд запущен на другом порту или хосте, измените URL соответственно.

### Шаг 3: Запустите бекенд

```bash
cd ../ruletka-backend

# Запустите PostgreSQL (если еще не запущен)
docker-compose up -d

# Запустите API сервер
make run
```

Бекенд будет доступен на `http://localhost:8080`

### Шаг 4: Запустите фронтенд

```bash
cd ../ruletka
npm run dev
```

## 🎮 Использование

### 1. Оффлайн режим (по умолчанию)

При первом запуске приложение работает в **оффлайн режиме**:
- 📴 Показывает badge "Оффлайн"
- Игра работает локально на клиенте
- Баланс не сохраняется на сервере

### 2. Онлайн режим (после входа)

1. Нажмите **"🔐 Войти"** в верхней панели
2. Выберите **"Регистрация"** (для нового аккаунта) или **"Вход"**
3. Введите email и пароль (минимум 6 символов)

После успешного входа:
- 🌐 Badge изменится на "Онлайн"
- Баланс синхронизируется с сервером
- Все спины будут отправляться на бекенд
- Результаты генерируются на сервере

### 3. Пополнение баланса (только онлайн)

1. Нажмите **"💰 Пополнить"**
2. Введите сумму
3. Нажмите **"Пополнить"**

Баланс обновится на сервере и отобразится в интерфейсе.

### 4. Игра

В **онлайн режиме**:
- Каждый спин отправляет запрос на `/game/line/spin`
- Результаты получаются от сервера
- Баланс автоматически обновляется

В **оффлайн режиме**:
- Игра работает локально
- Результаты генерируются на клиенте

## 🔍 Проверка работы API

### Health Check

Проверьте что бекенд работает:

```bash
curl http://localhost:8080/health
```

Должен вернуть: `{"status":"healthy"}`

### Тестовый запрос

Зарегистрируйте пользователя:

```bash
curl -X POST http://localhost:8080/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"password123"}'
```

## 🐛 Решение проблем

### Проблема: "Network Error"

**Причины:**
- Бекенд не запущен
- Неверный URL в `.env`
- CORS проблемы

**Решение:**
1. Проверьте что бекенд запущен: `curl http://localhost:8080/health`
2. Проверьте `VITE_API_URL` в `.env`
3. Перезапустите dev сервер после изменения `.env`

### Проблема: Токен не сохраняется

**Причины:**
- localStorage заблокирован браузером
- Приватный режим браузера

**Решение:**
- Используйте обычный режим браузера
- Проверьте настройки безопасности браузера

### Проблема: "unauthorized" после входа

**Причины:**
- Токен истек
- Бекенд перезапущен

**Решение:**
- Выйдите и войдите снова
- Проверьте время жизни токена на бекенде

## 📝 Структура API файлов

```
ruletka/src/shared/api/
├── client.ts          # Axios клиент с JWT
├── types.ts           # TypeScript типы
├── auth.ts            # Авторизация
├── user.ts            # Баланс и депозит
├── game.ts            # Спины
└── index.ts           # Экспорты

ruletka/src/features/auth/
├── model/store.ts     # Auth state
└── ui/AuthModal.tsx   # UI авторизации

ruletka/src/widgets/UserPanel/
└── ui/UserPanel.tsx   # Панель пользователя
```

## 🔐 Безопасность

- JWT токен хранится в `localStorage`
- Токен автоматически добавляется к запросам
- При ошибке 401 токен очищается
- Пароли никогда не хранятся на клиенте

## 📚 Дополнительно

Полная документация: [API_INTEGRATION.md](./API_INTEGRATION.md)

Backend документация: [../ruletka-backend/README.md](../ruletka-backend/README.md)

API примеры: [../ruletka-backend/api_examples.http](../ruletka-backend/api_examples.http)

## ✅ Готово!

Теперь ваше приложение полностью интегрировано с бекендом! 🎉

Используйте:
- **Оффлайн режим** для быстрой разработки и тестирования
- **Онлайн режим** для реальной игры с сохранением прогресса

